#pragma once

#include "turing.h"

// turing machine 1
bool accept1(const int);
struct delta_result delta1(struct delta_param);

// turing machine 2
bool accept2(const int);
struct delta_result delta2(struct delta_param);

// turing machine 3
bool accept3(const int);
struct delta_result delta3(struct delta_param);

// turing machine 4
bool accept4(const int);
struct delta_result delta4(struct delta_param);

// turing machine 5
bool accept5(const int);
struct delta_result delta5(struct delta_param);

// turing machine 6
bool accept6(const int);
struct delta_result delta6(struct delta_param);
